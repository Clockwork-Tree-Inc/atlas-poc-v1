"""Trust-layer recovery package (additive over the core `realid` recovery anchor).

This package holds the branch's recovery/biometric-storage building blocks — starting
with the threshold biometric-key seal (`threshold_seal`) that decouples storage from
confidentiality (ciphertext-anywhere). See `TRUST_LAYER.md` for the design of record.
"""
